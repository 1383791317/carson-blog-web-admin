export interface AuthLogin {
    account: string;
    password: string;
  }
  export interface RefreshToken {
    token: string;
  }
  export interface UserInfo {
    username: string;
    account: string;
  }